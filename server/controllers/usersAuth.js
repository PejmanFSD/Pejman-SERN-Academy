const usersModel = require("../models/users");

const registerUser = async (req, res) => {
    // validation
    // hash password
    // call usersModel.createUser()
};

module.exports = {
    registerUser
};